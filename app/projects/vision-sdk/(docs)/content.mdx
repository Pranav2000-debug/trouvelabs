import Image from "next/image";
import { Badge } from "@/components/ui/badge";

<Eyebrow id="Intro">Introduction</Eyebrow>

<div className="mb-4 flex flex-wrap items-center gap-3">
  <h1 className="text-3xl font-bold tracking-tight text-foreground sm:text-4xl lg:text-5xl">Vision SDK</h1>
  <Badge version="v1.0" />
</div>

<Lead>Vision SDK turns a single config file into a production video pipeline - inputs, models, tracking, and outputs - running on NVIDIA DeepStream.</Lead>

Define your input streams, AI models, object tracker, and outputs in a single YAML config file, then run everything with one command.

<figure className="mb-10 rounded-xl border border-border overflow-hidden">
  <Image
    src="/projects/vision-sdk/visionsdk-pipe.png"
    alt="Vision SDK pipeline overview - inputs (live streams, video files) flow through models (detector, tracker, classifier) into outputs (display, file recording, RTSP/RTMP streams, Kafka, FPS counter), all configured via a single YAML file."
    width={1280}
    height={800}
    loading="lazy"
    className="h-auto w-full"
    sizes="(min-width: 1280px) 768px, 100vw"
  />
</figure>

## Where to next

- [Prerequisites](/projects/vision-sdk/quickstart#prerequisites) - what you need installed
- [Installation](/projects/vision-sdk/quickstart#installation) - drop the wheel into DeepStream
- [Quick start](/projects/vision-sdk/quickstart#quick-start) - your first pipeline in three steps
