// TODO: Add docs sidebar with links (Overview, Quickstart, API Reference) — separate task.
export default function VisionSdkDocsLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
